{
  "name": "@semantic-forge/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure TypeScript domain invariants (scaffold)",
  "scripts": {
    "build": "echo \"domain build — PR2\""
  }
}
